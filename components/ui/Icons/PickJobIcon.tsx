import React from "react";
import PublishIcon from "@material-ui/icons/Publish";
import { green } from "@material-ui/core/colors";

const PickJobIcon = () => {
  return <PublishIcon style={{ color: green.A400 }} />;
};

export default PickJobIcon;
